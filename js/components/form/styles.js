import {
  Dimensions,
  StyleSheet,
} from 'react-native';


const styles = StyleSheet.create({
  content: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'space-around',
  },

  title: {
    fontSize: 35,
    color: 'black',
  },

  button: {
    marginLeft: 10,
    marginRight: 10,
  },
});

export default styles;
